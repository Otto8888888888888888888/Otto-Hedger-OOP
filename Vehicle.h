using namespace std;

class Vehicle{
    private:
    time_t timeOfEntry;
    int ID;
    public:


};
Vehicle::Vehicle(time_t s_timeOfEntry, int s_ID)
{
    timeOfEntry = s_timeOfEntry;
    ID = s_ID
}